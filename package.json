{
  "name": "verifymate",
  "version": "1.0.0",
  "description": "VerifyMate — does your app actually work? Active functional + deliverability verification.",
  "scripts": {
    "dev": "node server.js"
  },
  "dependencies": {
    "@sparticuz/chromium": "^123.0.0",
    "puppeteer-core": "^22.0.0"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
